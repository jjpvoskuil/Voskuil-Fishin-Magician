streamlit>=1.36
# Pinned below 1.4.0: starlette 1.4.0 added a required thread_minimum_size
# kwarg to its internal GZipResponder that streamlit's vendored gzip wrapper
# (streamlit/web/server/starlette/starlette_gzip_middleware.py) doesn't pass,
# crashing every page with "GZipResponder.__init__() missing 1 required
# keyword-only argument: 'thread_minimum_size'" before app code even runs.
# Confirmed reproducible with a bare-minimum app; see
# https://github.com/streamlit/streamlit/issues/16341 - open as of 2026-08-05.
# Verified working combo: streamlit==1.61.0 + starlette==1.3.1.
starlette<1.4.0
requests>=2.31
# Ensures zoneinfo (core/weather.py's lake_today()) can resolve "America/Chicago"
# even on minimal Linux images that don't ship a system tzdata database - some
# Streamlit Community Cloud build images don't.
tzdata
pandas>=2.0
folium>=0.16
streamlit-folium>=0.22
scikit-image>=0.22
scipy>=1.11
