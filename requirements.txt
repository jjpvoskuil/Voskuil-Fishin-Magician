streamlit>=1.36
# Pinned below 1.4.0: starlette 1.4.0 added a required thread_minimum_size
# kwarg to its internal GZipResponder that streamlit's vendored gzip wrapper
# (streamlit/web/server/starlette/starlette_gzip_middleware.py) doesn't pass,
# crashing every page with "GZipResponder.__init__() missing 1 required
# keyword-only argument: 'thread_minimum_size'" before app code even runs.
# Confirmed reproducible with a bare-minimum app; see
# https://github.com/streamlit/streamlit/issues/16341 - open as of 2026-08-05.
# Verified working combo: streamlit==1.61.0 + starlette==1.3.1.
starlette<1.4.0
requests>=2.31
# core/cabelas_lookup.py (punch-list #21/#22) uses curl_cffi instead of
# plain requests for its two live Cabela's/Coveo calls specifically because
# it can impersonate a real Chrome browser's TLS/JA3 fingerprint, not just
# its HTTP headers - a plain requests call with a browser User-Agent still
# came back empty from this app's own deployed server. Ships a compiled
# libcurl-impersonate binding (manylinux wheel), not a pure-Python package.
curl_cffi>=0.7
# Ensures zoneinfo (core/weather.py's lake_today()) can resolve "America/Chicago"
# even on minimal Linux images that don't ship a system tzdata database - some
# Streamlit Community Cloud build images don't.
tzdata
pandas>=2.0
# core.ui.render_line_chart() (punch-list #20) calls st.altair_chart()
# directly for the fixed-Y-axis temperature charts - previously altair only
# ever arrived as streamlit's own transitive dependency (used internally by
# st.line_chart), so this app never needed to import it itself until now.
altair>=5
folium>=0.16
streamlit-folium>=0.22
scikit-image>=0.22
scipy>=1.11
# Lure Inventory's "Scan a lure" photo-identify feature (core/lure_vision.py).
# Only actually called if ANTHROPIC_API_KEY is configured in Streamlit
# secrets - the import is wrapped in a try/except there either way, so a
# missing/outdated version here just disables that one feature, not the app.
anthropic>=0.40
